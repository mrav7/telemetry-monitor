# Apache Maven Wrapper configuration.
#
# The Maven runtime used by this project is pinned to an exact version and an
# exact Maven Central URL so that every local and CI build resolves the same
# distribution. Do not replace the version with a moving reference.
#
# distributionSha256Sum is verified by mvnw before the distribution is unpacked.
# Apache publishes SHA-512 and SHA-1 for this artifact; the SHA-256 below was
# computed from the distribution after validating it against the official
# SHA-512. Update it together with distributionUrl whenever Maven is upgraded.
distributionUrl=https://repo.maven.apache.org/maven2/org/apache/maven/apache-maven/3.9.16/apache-maven-3.9.16-bin.zip
distributionSha256Sum=5af3b743dd8b876b5c45da33b676251e5f1687712644abb4ee519ca56e1d89ce
